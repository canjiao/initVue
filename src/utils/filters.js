import utilsFn from '@/utils/utilsFn'

//格式化金额
export function formatAmount (amount) {
    return utilsFn.formatAmount(amount);
}
//格式化数量
export function toFixed (amount) { 
    return utilsFn.toFixed(amount);
}
